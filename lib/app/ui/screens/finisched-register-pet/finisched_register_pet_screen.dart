import 'package:flutter/material.dart';

import '../../templates/templates.dart';

class FinischedInsertDataScreen extends StatelessWidget {
  const FinischedInsertDataScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FinischedRegisterPetTemplate();
  }
}
