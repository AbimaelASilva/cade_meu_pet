import 'package:flutter/material.dart';

import '../../templates/templates.dart';

class RegisterPetcreen extends StatelessWidget {
  const RegisterPetcreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const RegisterPetTemplate();
  }
}
